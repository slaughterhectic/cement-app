services:
  - type: web
    name: cementbook-api
    runtime: node
    buildCommand: npm install && cd client && npm install && npx vite build
    startCommand: npx tsx server/index.ts
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: NODE_ENV
        value: production

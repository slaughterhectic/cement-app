# Backend (Render)
DATABASE_URL=postgresql://user:pass@host:5432/dbname
FRONTEND_URL=https://your-app.vercel.app
PORT=3001

# Frontend (Vercel) - prefix with VITE_ so Vite exposes it
VITE_API_URL=https://your-app.onrender.com/api
